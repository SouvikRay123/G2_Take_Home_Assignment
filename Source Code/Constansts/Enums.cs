namespace Constants
{
    public enum zoom_report_generation_step
    {
        get_latest_report, fetch_zoom_data, save_zoom_data, generate_report, update_status, create_next_day_entry
    }

    public enum zoom_metrics_type
    {
        meetings, zoom_rooms, webinars 
    }
}
